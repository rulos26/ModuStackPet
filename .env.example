# Nombre de la aplicación, utilizado en notificaciones y otros lugares.
APP_NAME=ModuStackPet

# Entorno de la aplicación: puede ser local, production, staging, etc.
APP_ENV=local

# Clave de encriptación de la aplicación. No compartas esta clave.
APP_KEY=base64:oPJc5w8hLFGqxa8A1fP8OzY7/RRmIZ30vKVfqk2XrR4=

# Activa o desactiva el modo de depuración. True muestra errores detallados.
APP_DEBUG=true

# URL base de la aplicación. Útil para generar enlaces.
APP_URL=http://localhost

# Idioma predeterminado de la aplicación.
APP_LOCALE=es

# Idioma de respaldo si el idioma principal no está disponible.
APP_FALLBACK_LOCALE=es

# Idioma utilizado por Faker para generar datos ficticios.
APP_FAKER_LOCALE=es_ES

# Controlador para el modo de mantenimiento. Puede ser "file" o "cache".
APP_MAINTENANCE_DRIVER=file

# Almacén para el modo de mantenimiento (opcional, descomentarlo si se usa).
# APP_MAINTENANCE_STORE=database

# Número de trabajadores para el servidor PHP CLI.
PHP_CLI_SERVER_WORKERS=4

# Número de rondas para el algoritmo Bcrypt (mayor número = más seguro, pero más lento).
BCRYPT_ROUNDS=12

# Canal de registro principal.
LOG_CHANNEL=stack

# Configuración del canal de registro en pila.
LOG_STACK=single

# Canal para registrar advertencias de deprecación.
LOG_DEPRECATIONS_CHANNEL=null

# Nivel de registro (debug, info, warning, error, etc.).
LOG_LEVEL=debug

# Configuración de la base de datos.
DB_CONNECTION=mysql       # Tipo de conexión (mysql, sqlite, etc.).
DB_HOST=127.0.0.1     # Dirección del servidor de base de datos.
DB_PORT=3306              # Puerto del servidor de base de datos.
DB_DATABASE=u494150416_B33pE # Nombre de la base de datos.
DB_USERNAME=u494150416_uJV3D # Usuario de la base de datos.
DB_PASSWORD=evuMy6dD8z    # Contraseña de la base de datos.

# Configuración de sesiones.
SESSION_DRIVER=database   # Controlador de sesiones (file, database, etc.).
SESSION_LIFETIME=120      # Duración de la sesión en minutos.
SESSION_ENCRYPT=false     # Si las sesiones deben ser encriptadas.
SESSION_PATH=/            # Ruta de las cookies de sesión.
SESSION_DOMAIN=null       # Dominio de las cookies de sesión.

# Configuración de transmisión de eventos.
BROADCAST_CONNECTION=log  # Conexión para transmisión (log, pusher, etc.).

# Configuración del sistema de archivos.
FILESYSTEM_DISK=local     # Disco predeterminado (local, s3, etc.).

# Configuración de colas.
QUEUE_CONNECTION=database # Conexión para colas (sync, database, etc.).

# Configuración de caché.
CACHE_STORE=database      # Almacén de caché (file, database, redis, etc.).
# CACHE_PREFIX=           # Prefijo para las claves de caché (opcional).

# Configuración de Memcached.
MEMCACHED_HOST=127.0.0.1  # Dirección del servidor Memcached.

# Configuración de Redis.
REDIS_CLIENT=phpredis     # Cliente de Redis (phpredis, predis).
REDIS_HOST=127.0.0.1      # Dirección del servidor Redis.
REDIS_PASSWORD=null       # Contraseña del servidor Redis (si aplica).
REDIS_PORT=6379           # Puerto del servidor Redis.

# Configuración de correo.
MAIL_MAILER=smtp          # Controlador de correo (smtp, log, etc.).
#MAIL_SCHEME=smtp.gmail.com         # Esquema del correo (opcional).
MAIL_HOST=smtp.gmail.com       # Dirección del servidor SMTP.
MAIL_PORT=587            # Puerto del servidor SMTP.
MAIL_USERNAME=rulos26@gmail.com       # Usuario para autenticación SMTP.
MAIL_PASSWORD=imltkpfnvehflplt        # Contraseña para autenticación SMTP.
MAIL_ENCRYPTION=tls     # Método de encriptación (tls, ssl, etc.).
MAIL_FROM_ADDRESS="rulos26@gmail.com" # Dirección de correo del remitente.
MAIL_FROM_NAME="${APP_NAME}"          # Nombre del remitente.

# Configuración de AWS (opcional).
AWS_ACCESS_KEY_ID=        # ID de clave de acceso de AWS.
AWS_SECRET_ACCESS_KEY=    # Clave secreta de AWS.
AWS_DEFAULT_REGION=us-east-1 # Región predeterminada de AWS.
AWS_BUCKET=               # Nombre del bucket de AWS S3.
AWS_USE_PATH_STYLE_ENDPOINT=false # Usar estilo de ruta para el endpoint.

# Configuración de Vite (herramienta de frontend).
VITE_APP_NAME="${APP_NAME}" # Nombre de la aplicación para Vite.
